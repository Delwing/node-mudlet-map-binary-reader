# Project Notes for Claude

## Package Manager

**Use `yarn`, not `npm`.**

- Install dependencies: `yarn`
- Add a dev dependency: `yarn add --dev <package>`
- Run tests: `yarn test`
- Run a script: `yarn <script>`

Never use `npm install`, `npm test`, or `npm run`.
